name: Validate userscript v2.6.0

on:
  push:
    paths:
      - payload/userscript-2.6.0.patch01.txt
      - .github/workflows/validate-userscript-v2.6.0.yml

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate patch syntax
        run: |
          cp payload/userscript-2.6.0.patch01.txt /tmp/userscript-2.6.0.patch01.js
          node --check /tmp/userscript-2.6.0.patch01.js
